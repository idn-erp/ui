import { ObjectToTimePipe } from './object-to-time.pipe';

describe('ObjectToTimePipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectToTimePipe();
    expect(pipe).toBeTruthy();
  });
});
